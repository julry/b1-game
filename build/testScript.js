let groundFloor;

function setup() {
    createCanvas(800, 400);
    new Sprite();
    // world.gravity.y = 10;
    // groundFloor = new window.p5.Sprite(0, height - 30, width * 2, 20, 'static');
}

function draw() {
    background(145, 125, 0);
}